#root {
  width: 100%;
}

.navbar {
  display: flex;
  background: rgba(0, 0, 0, 0.3);
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  padding: 0 5%;
  height: 70px;
  z-index: 1000;
  /* Quan trọng: Ngăn chặn layout shift do thanh cuộn */
  scrollbar-gutter: stable; 
  /* Đảm bảo box-sizing không làm thay đổi size khi có border/padding */
  box-sizing: border-box;
}
.nav-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
}

.nav-content.container {
  margin: 0;
}

.logo {
  text-decoration: none;
  color: inherit;
  display: flex;
  align-items: center;
  gap: 12px;
  z-index: 1100;
}

.logo-icon {
  background: var(--primary);
  padding: 8px;
  border-radius: 12px;
  display: flex;
}

.logo-text {
  font-size: 1.4rem;
  font-weight: 900;
  letter-spacing: -0.02em;
}

/* Nút Hamburger cho Mobile */
.nav-toggle {
  display: none;
  background: none;
  border: none;
  color: var(--text-main);
  cursor: pointer;
  z-index: 1100;
}

/* Menu Navbar */
.nav-menu {
  display: flex;
  align-items: center;
  gap: 24px;
}

.nav-links {
  display: flex;
  gap: 8px;
}

.nav-link {
  text-decoration: none;
  color: var(--text-muted);
  font-weight: 600;
  font-size: 0.95rem;
  padding: 8px 16px;
  border-radius: 10px;
  transition: 0.2s;
}

.nav-link:hover {
  color: var(--primary);
  background: rgba(79, 70, 229, 0.05);
}

.nav-auth-group {
  display: flex;
  gap: 12px;
  align-items: center;
}

/* RESPONSIVE NAVBAR */
@media (max-width: 992px) {
  .nav-toggle {
    display: block;
  }

  .nav-menu {
    position: fixed;
    top: 0;
    right: -100%;
    width: 280px;
    height: 100vh;
    background: var(--white);
    flex-direction: column;
    padding: 100px 24px;
    box-shadow: -10px 0 30px rgba(0, 0, 0, 0.05);
    transition: 0.3s ease;
    align-items: flex-start;
    z-index: 10;
  }

  .nav-menu.is-open {
    right: 0;
  }

  .nav-links {
    flex-direction: column;
    width: 100%;
  }

  .nav-link {
    width: 100%;
  }

  .nav-auth-group {
    width: 100%;
    flex-direction: column;
    align-items: stretch;
  }
}

.footer {
  padding: 60px 0 40px;
  text-align: center;
  border-top: 1px solid var(--border);
  color: var(--text-light);
  margin-top: 80px;
}


#root { width: 100%; }

.navbar {
  display: flex;
  background: rgba(5, 7, 10, 0.8); /* Nền tối trong suốt */
  backdrop-filter: blur(12px);    /* Hiệu ứng mờ kính */
  position: fixed;
  top: 0; left: 0; width: 100%; height: 70px;
  padding: 0 5%;
  z-index: 1000;
  border-bottom: 1px solid var(--border);
}

.nav-content {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
}

.logo-icon {
  background: var(--primary);
  box-shadow: var(--gold-glow);
  padding: 8px;
  border-radius: 12px;
  display: flex;
}

.logo-text {
  font-size: 1.4rem;
  font-weight: 900;
  color: var(--primary);
  letter-spacing: -0.02em;
}

.nav-link {
  text-decoration: none;
  color: var(--text-muted);
  font-weight: 600;
  padding: 8px 16px;
  transition: 0.2s;
}

.nav-link:hover {
  color: var(--primary);
  background: rgba(212, 175, 55, 0.1); /* Hover màu vàng gold nhạt */
}

.footer {
  padding: 60px 0 40px;
  text-align: center;
  /* border-top: 1px solid var(--border); */
  border: 0;
  color: var(--text-light);
  background: rgba(0, 0, 0, 0.3);
  margin-top: 80px;
}
